{% extends "layout/intranet/template_lite.twig" %}
{% block head %}
  <link type="text/css" rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.9.0/slick.min.css"  media="screen"/>
  <link type="text/css" rel="stylesheet" href="https://cdn.jsdelivr.net/npm/easyzoom-mp@2.2.2/css/easyzoom.css"  media="screen"/>
  <style type="text/css">
    .btn-flat-not:hover{background-color: #21478F ;color: white !important;}
    .sliderMobile .slick-list {padding:0px !important;}
    .sliderMobile .slick-dots li.slick-active button:before{font-size: 70px !important;opacity: 1;color: {{configuracion.Con_ColorBtn}};}
    .sliderMobile img{max-width:100%;max-height: 250px;}
    .sliderMobile{height:250px;}
    .materialbox-caption{text-transform: uppercase;font-size: 1.5rem;}
  </style>
{% endblock %}
{% block content %}
  <main class="container fixContainer positionRelative">
    <br class="hide-on-large-only">
    <br class="hide-on-large-only">
    <div class="row centrarForzado">
      <div class="card">
        <div class="card-content sinPadding">
          <div class="row cardContentFix anchoTotal">
            <div class="col s12 m12 l10 hide-on-med-and-down">
              <a href="//pormayor.pe/categorias/{{producto.urlCategoria}}" class="pormayorUnico-text breadcrumb">{{producto.Cat_Nombre|title}}</a>
              <a href="//pormayor.pe/subcategoria/{{producto.urlSubCategoria}}" class="centrarForzado pormayorUnico-text breadcrumb">{{producto.Suc_Nombre}}</a>
              <a href="//pormayor.pe/detalle/{{producto.urlDetalle}}" class="centrarForzado pormayorUnico-text breadcrumb">{{producto.Des_Nombre}}</a>
              <ol class="hide" itemscope itemtype="http://schema.org/BreadcrumbList">
                <li itemprop="itemListElement" itemscope itemtype="http://schema.org/ListItem">
                  <a href="//pormayor.pe/categorias/{{producto.urlCategoria}}" itemprop="item" class="pormayorUnico-text breadcrumb"><span itemprop="name">{{producto.Cat_Nombre}}</span></a>
                  <meta itemprop="position" content="1" />
                </li>
                <li itemprop="itemListElement" itemscope itemtype="http://schema.org/ListItem">
                  <a href="//pormayor.pe/subcategoria/{{producto.urlSubCategoria}}" itemprop="item" class="pormayorUnico-text breadcrumb "><span itemprop="name">{{producto.Suc_Nombre}}</span></a>
                  <meta itemprop="position" content="2" />
                </li>
                <li itemprop="itemListElement" itemscope itemtype="http://schema.org/ListItem">
                  <a href="//pormayor.pe/detalle/{{producto.urlDetalle}}" itemprop="item" class="pormayorUnico-text breadcrumb "><span itemprop="name">{{producto.Des_Nombre}}</span></a>
                  <meta itemprop="position" content="3" />
                </li>
              </ol>
              {% if userAgent == "PC" %}
                {% if usuario.logueado %}
                  {% if producto_favorito %}
                    <div class="star seleccionado right comoBoton" style="padding-left:10px"  data-id_producto="{{producto.Pro_IdProducto}}" data-valor="1">
                      <img alt="Pormayor.pe - favoritos" src="{{ruta_img}}fav.svg" type="image/svg+xml"></img>
                    </div>
                  {% else %}
                    <div class="star right comoBoton" style="padding-left:10px" data-id_producto="{{producto.Pro_IdProducto}}" data-valor="0">
                      <img alt="Pormayor.pe - favoritos" src="{{ruta_img}}fav.svg" type="image/svg+xml"></img>
                    </div>
                  {% endif %}
                {% endif %}
              {% endif %}
              <span style="{% if usuario.logueado %}border-right: 1.5px solid rgba(0,0,0,0.2);{% endif %}padding:0px 10px" class="pormayorUnico-text right">{{vista.Pro_Vista}} vistas</span>
              {% if range_producto %}
                <a class='dropdown-button sinPadding right valign-wrapper grey-text text-darken-2' href='#' data-activates='calificacion3' style="border-right: 1px solid rgba(0,0,0,0.2);"><img alt="Pormayor.pe - Rate" class="positionRelative left" src="{{ruta_img}}califA.png" alt="calificacion tienda pormayor.pe" height="20" >&nbsp;&nbsp;&nbsp; {{range_producto.positivo|round}}%<i class="material-icons">arrow_drop_down</i></a>
                
                <ul id='calificacion3' class='dropdown-content'>
                  <li style="border-left:6px solid #4caf50" ><a  href="#!"><img alt="Pormayor.pe - Rate" class="left positionRelative" src="{{ruta_img}}califA.png" alt="calificacion tienda pormayor.pe" height="25">&nbsp;&nbsp;&nbsp;EL <b>{{range_producto.positivo|round}}%</b> LO RECOMIENDA</a></li>
                  <li style="border-left:6px solid #ffc107"><a  href="#!"><img alt="Pormayor.pe - Rate" class="left positionRelative" src="{{ruta_img}}califB.png" alt="calificacion tienda pormayor.pe" height="25">&nbsp;&nbsp;&nbsp;EL <b>{{range_producto.neutral|round}}%</b> NO OPINA SOBRE EL</a></li>
                  <li style="border-left:6px solid #f44336"><a href="#!"><img alt="Pormayor.pe - Rate" class="left positionRelative" src="{{ruta_img}}califC.png" alt="calificacion tienda pormayor.pe" height="25">&nbsp;&nbsp;&nbsp;EL <b>{{range_producto.negativo|round}}%</b> NO LO RECOMIENDO</a>
                </ul>
              {% endif %}
            </div>
            {% if userAgent == "PC" %}
              <div class="col s12 m12 l2 hide-on-med-and-down" style="border-left: 1.5px solid rgba(0,0,0,0.2)">
                <div class="fb-like left" data-href="http://pormayor.pe/productos/dp/{{url}}?fb_ref=top_left&fb_source=profile_oneline" data-layout="button_count" data-action="like" data-size="small" data-show-faces="true" data-share="true"></div>
              </div>
            {% endif %}
            <div class="col s12 m12 l12 hide-on-med-and-down">
              <h1 class="fixH1Seo centrarForzado enfasisA muayuscula">{{producto.Pro_Nombre|title}}</h1>
              <br>
              <div class="divider"></div>
            </div>
            <div class="col s12 {% if producto.Pro_Caracteristicas %} l4 {% else %} l7 {% endif %} center hide-on-med-and-down" id="imagenesPro" >
              {% if fotos is empty %}
                <div class="easyzoom easyzoom--overlay easyzoom--with-thumbnails redondeo white">
                  <a href="{{ruta_img}}pronoimg.webp">
                    <img alt="{{producto.Pro_Nombre}} al pormayor" src="{{ruta_img}}pronoimg.webp" class="containImg anchoTotal"/>
                  </a>
                </div>
              {% else %}
                <div class="easyzoom easyzoom--overlay easyzoom--with-thumbnails redondeo white">
                  <a href="{{ruta_img}}{{fotos[0].Prf_Img}}">
                    <img alt="{{producto.Pro_Nombre}} al pormayor" src="{{ruta_img}}{{fotos[0].Prf_Img}}" class="containImg anchoTotal"/>
                  </a>
                </div>
              {% endif %}
              <ul class="thumbnails">
                {% if fotos is empty %}
                  <li class="col">
                    <a href="{{ruta_img}}pronoimg.webp" data-standard="{{ruta_img}}pronoimg.webp">
                      <img alt="{{producto.Pro_Nombre}} al pormayor" src="{{ruta_img}}pronoimg.webp" class="redondeo" height="40">
                    </a>
                  </li>
                {% else %}
                    {% for fot in fotos %}
                      {% if producto.Pro_IdProducto == fotos[0].producto_Pro_IdProducto %}
                        {% if fot.Prf_Img|slice(0, 5)|lower != "nopic" %}
                        <li class="col">
                          <a href="{{ruta_img}}{{fot.Prf_Img}}" data-standard="{{ruta_img}}{{fot.Prf_Img}}">
                            <img alt="{{producto.Pro_Nombre}} al pormayor" src="{{ruta_img}}{{fot.Prf_Img}}" class="redondeo" height="40">
                          </a>
                        </li>
                        {% endif %}
                      {% endif %}
                    {% endfor %}
                {% endif %}
              </ul>
            </div>
            <div class="col s12 l6 center show-on-medium-and-down hide-on-large-only">
              <div class="wrapperSliderImgMobile hide transition">
                <div class="sliderMobile ">
                  {% for fot in fotos %}
                    {% if producto.Pro_IdProducto == fotos[0].producto_Pro_IdProducto %}
                      {% if fot.Prf_Img|slice(0, 5)|lower != "nopic" %}
                        <div class="">
                          <img alt="{{producto.Pro_Nombre}} al pormayor" class="redondeo centrarForzado" src="{{ruta_img}}{{fot.Prf_Img}}">
                        </div>
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                </div>
              </div>
              <div class="preloader-wrapper big active preloadImgMobile">
                <div class="spinner-layer" style="border-color: #FEC00F">
                  <div class="circle-clipper left">
                    <div class="circle"></div>
                  </div>
                  <div class="gap-patch">
                    <div class="circle"></div>
                  </div><div class="circle-clipper right">
                    <div class="circle"></div>
                  </div>
                </div>
              </div>
              <br>
              <br>
            </div>
            {% if userAgent != "PC" %}
              <div class="col s12 l6 center show-on-medium-and-down hide-on-large-only">
                <h1 class="fixH1Seo centrarForzado enfasisA muayuscula">{{producto.Pro_Nombre|title}}</h1>
                <br>
              </div>
            {% endif %}
            {% if userAgent != "PC" %}
            <div class="col s12 m12 l2 center">
              <span class="pormayorUnico-text right">{{vista.Pro_Vista}} vistas</span>
              {% if usuario.logueado %}
                {% if producto_favorito %}
                  <div class="star seleccionado left"  data-id_producto="{{producto.Pro_IdProducto}}" data-valor="1">
                    <img alt="Pormayor.pe - favoritos" src="{{ruta_img}}fav.svg" type="image/svg+xml"></img>
                  </div>
                {% else %}
                  <div class="star left" data-id_producto="{{producto.Pro_IdProducto}}" data-valor="0">
                    <img alt="Pormayor.pe - favoritos" src="{{ruta_img}}fav.svg" type="image/svg+xml"></img>
                  </div>
                {% endif %}
              {% endif %}
                <div class="fb-like" data-href="http://{{producto.Tie_Subdominio}}.pormayor.pe/tienda/productos/dp/{{url}}" data-layout="button_count" data-action="like" data-size="small" data-show-faces="true" data-share="true"></div>
            </div>
            {% endif %}
            <div class="row centrarForzado hide-on-large-only">
              <div class="divider"></div>
            </div>
            <div itemscope itemtype="http://schema.org/Product" class="hide">
              <img itemprop="image" alt="{{producto.Pro_Nombre}} al pormayor" src="{{ruta_img}}{{fotos[0].Prf_Img}}" class="containImg" />
              <span itemprop="name" class="muayuscula">{{producto.Pro_Nombre}}</span>
              <p itemprop="description">{{producto.Pro_Descripcion|replace({'<figure class="image">': '', '</figure>' : '', '<img src="https://cdn.shopify.com/s/files/1/1415/1124/products/1138-Auto-Electrico-Porsche-6V-V2_1024x1024.jpg?v=1528385082">' : '', '<img src="https://cdn.shopify.com/s/files/1/1415/1124/products/1138-Auto-Electrico-Porsche-6V-V2_1024x1024.jpg?v=1528385082">': ''})}}</p>
              <a itemprop="isRelatedTo" href="http://{{producto.Tie_Subdominio}}.pormayor.pe"></a>
              <div itemprop="offers" itemscope itemtype="http://schema.org/AggregateOffer">
                <span itemprop="lowPrice">{{producto.Pro_PrecioMinimo}}</span>
                <span itemprop="highPrice">{{producto.Pro_PrecioMaximo}}</span>
                 <meta itemprop="priceCurrency" content="S/" />
              </div>
            </div>
            {% if userAgent == "PC" %}
              {% if producto.Pro_Caracteristicas %}
                <div class="col s12 l3">
                  {% set caracteristicas = producto.Pro_Caracteristicas|split('|') %}
                  <ul class="caracteristicasLista">
                    {% for cat in caracteristicas %}
                    <li class="enfasisB fontB2"><i class="positionRelative material-icons fontB2 grey-text text-darken-4">check</i>&nbsp;&nbsp;&nbsp; {{cat}}</li>
                    {% endfor %}
                  </ul>
                </div>
              {% endif %}
            {% endif %}
            <div class="col s12 l5 center" style="margin-top:15px">
              <div class="row centrarForzado">
                  {% if colores is empty %}
                  {% else %}
                  <div class="col s2 sinPadding left-align">
                    <span>Colores</span>
                  </div>
                  <div class="col s10 sinPadding left-align">
                    {% set arrayColores = [] %}
                    {% for col in colores %}
                      {% if col.producto_Pro_IdProducto == pro.Pro_IdProducto %}
                       {% if col.Col_Hex not in arrayColores %}
                       {% set arrayColores = arrayColores|merge([col.Col_Hex]) %}
                          <div class="colorSample" style="background-color: {{col.Col_Hex}};"></div>
                       {% endif %}
                      {% endif %}
                    {% endfor %}
                  </div>
                  {% endif %}
                {% if userAgent == "PC" %}
                <div class="col s12 card-title">Elige como comprar</div>
                <div class="wrapperSliderPrecios hide transition">
                  <div class="col l12 sinPadding left-align">
                    <div class="row centrarForzado positionRelative" >
                        {% for pre in precios %}
                          {% if loop.index == 1 %}
                            <a class="irComprarBtn" href="#compraBlock">
                              <div class="col s6 m6 l6 center green lighten-5 green-text negrita z-depth-2 redondeo" style="padding:5px;">
                                  <span class="fontA">S/ {{pre.Prp_Precios}}</span><br>
                                  <span>COMPRAR AHORA</span>
                              </div>
                            </a>
                        {% elseif loop.index == 3 %}
                        <a href="{{base_url()}}grupal/nuevo/{{url}}">
                          <div class="col s6 m6 l6 center green white-text negrita z-depth-2 redondeo" style="padding:5px;">
                              <span class="fontA">S/ {{pre.Prp_Precios}}</span><br>
                              <span>COMPRAR GRUPAL</span>
                          </div>
                        </a>
                        {% endif %}                          
                        {% endfor %}
                    </div>
                  </div>
                  <span>&nbsp;</span>
                  <div class="col l12 sinPadding left-align">
                    <div class="row centrarForzado positionRelative " >
                      <a href="#visualizas" class="black-text tooltipped" data-position = "center" data-delay = "500"      data-tooltip = "Pago con Tarjeta de Crédito/Débito o Pago Contra Entrega">
                        <div class="col s6 m6 l6 center green lighten-5 green-text negrita z-depth-2 redondeo" style="padding:5px;">
                          <span class="black-text">
                            <i class="material-icons positionRelative" style="top:5px;">security</i>&nbsp;&nbsp;COMPRA SEGURA
                          </span>
                          <br>
                        </div>
                      </a>
                      
                      <a href="#visualizas">
                        <div class="VerEnvios col s6 m6 l6 center pormayor black-text negrita z-depth-2 redondeo" style="padding:5px;">
                          <span class="black-text">
                            <i class="material-icons positionRelative" style="top:5px;font-size:24px;">local_shipping</i>&nbsp;&nbsp;CALCULAR ENVÍO
                          </span>
                        </div>
                      </a>
                    </div>
                    <br>
                  </div>

                    <div class="cardContentFix" id="visualizas" style="display:none;"></div>
                    <div class="col l12" id="visualizarEnvio" style="display:none;">
                      <div class="row sinPadding">
                        <div class="col l6">
                          <select id="selecRegion" class="browser-default" data-valor="{{direccion_usuario.Pag_idregion}}">
                          <option value="0" disabled selected>Departamento</option>
                          {% for reg in region %}
                            <option value="{{reg.id}}">{{reg.name}}</option>
                          {% endfor %}
                          </select>
                        </div>
                        <div id="provincia" class="col l6">
                          <select  disabled class="browser-default">
                            <option value="0" disabled selected> Seleccione una provincia</option>
                          </select>
                        </div>
                        <div id="distrito" class="col l6">
                          <select disabled class="browser-default">
                            <option value="0" disabled selected> Seleccione un distrito</option>
                          </select>
                        </div>
                        <div class="col l6">
                          <select id="cantEnvio" class="browser-default">
                          <option value="0" disabled selected>Cantidad</option>
                            {% for i in 1..100 %}
                              <option value="{{i}}">{{i}}</option>
                            {% endfor %}
                          </select>
                        </div>
                      </div>
                      <div class="row sinPadding">
                        <div class="col l12 black-text negrita noenv tiempoEntrega" style="text-align: left;font-size:16px;">Tiempo de entrega de 2 a 3 días (Lima y Callao)</div>
                        <div class="col l12" style="text-align: right;">S/ <span class="cEnv fontC negrita">0.00</span></div>
                      </div>
                    </div>
                  </div>
                </div>
                <div class="preloader-wrapper big active preloadPrecios">
                  <div class="spinner-layer" style="border-color: #FEC00F">
                    <div class="circle-clipper left">
                      <div class="circle"></div>
                    </div><div class="gap-patch">
                      <div class="circle"></div>
                    </div><div class="circle-clipper right">
                      <div class="circle"></div>
                    </div>
                  </div>
                </div>
                {% else %}
                <div class="col s12 center">Elige como comprar</div>
                <div class="wrapperSliderPrecios hide transition">
                  <div class="col s12 sinPadding">
                    <div class="row centrarForzado positionRelative grey lighten-4 ">
                       {% for pre in precios %}
                          {% if loop.index == 1 %}
                            <a class="irComprarBtn" href="#compraBlock">
                              <div class="col s6 m6 l6 center green lighten-5 green-text negrita" style="padding:5px;">
                                  <span class="fontA">S/ {{pre.Prp_Precios}}</span><br>
                                  <span>COMPRAR AHORA</span>
                              </div>
                            </a>
                          {% elseif loop.index == 3 %}
                          <a href="{{base_url()}}grupal/nuevo/{{url}}">
                            <div class="col s6 m6 l6 center green white-text negrita" style="padding:5px;">
                                <span class="fontA">S/ {{pre.Prp_Precios}}</span><br>
                                <span>COMPRAR GRUPAL</span>
                            </div>
                          </a>
                        {% endif %}                          
                      {% endfor %}
                    </div>
                  </div>
                  <span>&nbsp;</span>
                  <div class="row centrarForzado">
                    <div class="row centrarForzado positionRelative" >
                      <a href="#visualizs">
                        <div class="VerEnvios col s12 m12 l12 left pormayor black-text negrita" style="padding:5px;">
                          <span class="fontD2">
                            <i class="material-icons positionRelative" style="top:5px;">local_shipping</i>&nbsp;&nbsp;CALCULAR ENVÍO
                          </span>
                          <br>
                        </div>
                      </a>
                    <br>
                    <br>
                  </div>
                
                    <div class="cardContentFix" id="visualizs" style="display:none;"></div>
                   
                    <div class="col12 s12 l12 row cardContentFix" id="visualizarEnvio" style="display:none;">
                      <div class="row sinPadding">
                        <div class="col12 l12">
                          <select id="selecRegion" class="browser-default" data-valor="{{direccion_usuario.Pag_idregion}}">
                          <option value="0" disabled selected>Departamento</option>
                          {% for reg in region %}
                            <option value="{{reg.id}}">{{reg.name}}</option>
                          {% endfor %}
                          </select>
                        </div>
                        <div id="provincia" class="col12 l12">
                          <select  disabled class="browser-default">
                            <option value="0" disabled selected> Seleccione una provincia</option>
                          </select>
                        </div>
                        <div id="distrito" class="col12 l12">
                          <select disabled class="browser-default">
                            <option value="0" disabled selected> Seleccione un distrito</option>
                          </select>
                        </div>
                        <div class="col12 l12">
                          <select id="cantEnvio" class="browser-default">
                          <option value="0" disabled selected>Cantidad</option>
                            {% for i in 1..100 %}
                              <option value="{{i}}">{{i}}</option>
                            {% endfor %}
                          </select>
                        </div>
                      </div>
                      <div class="row sinPadding">
                        <div class="col l12 centrarForzado">S/ <span class="cEnv fontC negrita">0.00</span></div>
                        <div class="col l12 black-text negrita noenv tiempoEntrega" style="text-align: left;font-size:13px;">Tiempo de entrega de 2 a 3 días (Lima y Callao)</div>
                      </div>
                    </div>
                  </div> 
                </div>
                <div class="preloader-wrapper big active preloadPrecios">
                  <div class="spinner-layer" style="border-color: #FEC00F">
                    <div class="circle-clipper left">
                      <div class="circle"></div>
                    </div><div class="gap-patch">
                      <div class="circle"></div>
                    </div><div class="circle-clipper right">
                      <div class="circle"></div>
                    </div>
                  </div>
                </div>   
                {% endif %}
              </div>
            </div>
          </div>
          {% if userAgent == "PC" %}
          <br>
          {% endif %}
          <div class="divider"></div>
          {% if userAgent == "PC" %}
          <div class="row cardContentFix" id="compraBlock">
            <div class="col s12 m12 l5 popper miniTutoPro" style="border-right: 1px solid rgba(0,0,0,0.2);">
              <p class="card-title center negrita">Agregue la cantidad y modelo</p>
              {% for key, sku in SKU %}  
                {% if producto.Pro_IdProducto == sku.Producto_Pro_IdProducto %}
                {% set colorActual = sku.SKU_Color %}
                  <div class="col s12">
                    <div class="row centrarForzado">
                      <div class="col l3 left-align">
                        <div class="positionRelative">
                          {% if  sku.SKU_Img == '' and producto.Prf_Img == '' %}
                            <img alt="{{producto.Pro_Nombre}} al pormayor" id="skuImg{{colorActual}}" data-color="{{colorActual}}-{{producto.Pro_IdProducto}}" data-caption="{{producto.Pro_Nombre}} {{colorActual|title}}" class=" materialboxed fixImg4 redondeo" src="{{ruta_img}}nofoto.png" >
                          {% elseif sku.SKU_Img == '' and producto.Prf_Img != '' %}
                            <img alt="{{producto.Pro_Nombre}} al pormayor" id="skuImg{{colorActual}}" data-color="{{colorActual}}-{{producto.Pro_IdProducto}}" data-caption="{{producto.Pro_Nombre}} {{colorActual|title}}" class=" materialboxed fixImg4 redondeo" src="{{ruta_img}}{{producto.Prf_Img}}" >
                          {% else %}
                            <img alt="{{producto.Pro_Nombre}} al pormayor" id="skuImg{{colorActual}}" data-color="{{colorActual}}-{{producto.Pro_IdProducto}}" data-caption="{{producto.Pro_Nombre}} {{colorActual|title}}" data-nom="{{colorActual}}" class=" materialboxed fixImg4 redondeo" src="{{ruta_img}}{{sku.SKU_Img}}" >
                          {% endif %}
                        {% set colorUnico = "" %}
                        {% set colorHex = " " %}
                        {% set break = false %}
                        {% for col in colores if not break %}
                          {% if col.SKU_Color == colorActual %}
                            <div class="colorEjemplo positionAbsolute bottom0" style="background-color:{{col.Col_Hex}}"></div>
                            {% set colorUnico = col.Col_Nombre %}
                            {% set colorHex = col.Col_Hex %}
                            {% set break = true %}
                          {% endif %}
                        {% endfor %}
                        </div>
                        <br>
                      </div> 
                      <div class="col l9 valign-wrapper containerCantidadPro">
                        <div class="col s9 sinPadding">
                          <span class="fontB2 enfasisB muayuscula">{{producto.Pro_Nombre|title}}</span> <i>{{colorUnico|title}}</i>
                        </div> 
                        <div class="col s3 input-field filaCant{{key}}">
                          {% set count = 0 %}
                          {% set break = false %}
                          {% for skub in SKUB|sort('asc') if not break  %}                        
                              {% if colorActual == skub.SKU_Color  %}
                                  <input type="number" min="{{precios[0].Prp_Desde}}" name="cantidad" class="center z-depth-2 redondeo grey darken-4 negrita white-text cantSku cant{{skub.SKU_IdSku}}" placeholder="Cantidad" id="{{skub.SKU_IdSku}}" data-idpro="{{producto.Pro_IdProducto}}" data-colresumen="{% if colorHex %}{{colorHex}}{% else %}unico{% endif %}" data-foto="{% if  sku.SKU_Img == '' and producto.Prf_Img == '' %}{{ruta_img}}nofoto.png{% elseif sku.SKU_Img == '' and producto.Prf_Img != '' %}{{ruta_img}}{{producto.Prf_Img}}{% else %}{{ruta_img}}{{sku.SKU_Img}}{% endif %}"  data-productname="{{producto.Pro_Nombre}}" data-productid="{{skub.SKU_IdSku}}" data-price="" xd="" data-tipovariacion="{{skub.Var_Nombre}}" data-color="{{skub.SKU_Color}}" data-variacion="{{skub.Vao_Nombre}}" data-img="{% if skub.SKU_Img %}{{skub.SKU_Img}}{% else %}{{producto.Prf_Img}}{% endif %}" data-unidad="{{skub.Unm_Nombre}}" data-idtienda="{{producto.Tie_IdTienda}}" data-minventa="{{precios[0].Prp_Desde}}" data-preciounit="" data-documento="{{producto.Pro_Documento}}" onkeydown="javascript: return event.keyCode == 69 ? false : true">
                                  <input type="number" min="0" id="precioUni{{skub.SKU_IdSku}}" class="hide center preUni" data-idsku="{{skub.SKU_IdSku}}" readonly="">
                                  <input type="number" min="0" id="precioPro{{skub.SKU_IdSku}}" class="hide center preImp" data-idsku="{{skub.SKU_IdSku}}" readonly="">
                                  {# <label for="{{skub.SKU_IdSku}}" class="black-text">Cantidad</label> #}
                              {% else %}
                                {% set break = false %} 
                              {% endif %} 
                          {% set count = count + 1 %}
                          {% endfor %}
                        </div>
                      </div>
                    </div>
                  </div>
                {% endif %}
              {% endfor %}
            </div>
            <div class="col l1">
            </div>
            <div class="col s12 m12 l6 sinPadding unidadSticky positionRelative centrarForzado">
              <p class="card-title center negrita">Mi resumen de compra</p>
              <br>
              <div class="row resumenContainer centrarForzado">
                <div class="emptyResumen">
                  <div class="center col s12 m12 l12" >
                    <img src="{{ruta_img}}emptyProductos.webp" height="150"></img>
                  </div>
                  <div class="center col s12 m12 l12">
                    <h5>Sin productos</h5>
                    <span class="enfasisB">Aquí va tu lista de compras...</span>
                  </div>
                </div>
                <div class="resumenPrecios"></div>
              </div>
                <div class="prevResumen align-wrapper comoBoton sinUser positionAbsolute hide"><i class=" material-icons fontA enfasisA">keyboard_arrow_up</i></div>
                <div class="nextResumen valign-wrapper comoBoton sinUser positionAbsolute hide"><i class=" material-icons fontA enfasisA">keyboard_arrow_down</i></div>
              <div class="row">
                {% if usuario.id_tienda != producto.Tie_IdTienda %}
                  <div class="col s7 sinPadding center valign-wrapper wrapperPrecioMobile">
                    <a class="centrarForzado btn green add_cart sinHover" data-totalc="{{totalc}}">AGREGAR AL CARRITO</a>
                  </div>
                  <div class="col s5 sinPadding right-align valign-wrapper wrapperPrecioMobile positionRelative">
                    <span class="centrarForzado fontB22 positionAbsolute "><strong id="totalcantidad">0</strong> unidades</span>
                    <span class="centrarForzado">S/. <strong class="fontC" id="totalImporte">0</strong></span>
                  </div>
                {% endif %}
              </div>
            </div>
          </div>
          {% else %}
          <div class="row cardContentFix" id="compraBlock">
            <div class="col s12 m12 l5 popper miniTutoPro sinPadding">
              <p class="card-title center negrita">Selecciona la cantidad y modelo</p>
              <br>
              {% for key, sku in SKU %}  
                {% if producto.Pro_IdProducto == sku.Producto_Pro_IdProducto %}
                {% set colorActual = sku.SKU_Color %}
                  <div class="col s12 sinPadding">
                    <div class="row centrarForzado">
                      <div class="col s3 left-align valign-wrapper" style="height:100px">
                        <div class="positionRelative">
                          {% if  sku.SKU_Img == '' and producto.Prf_Img == '' %}
                            <img alt="{{producto.Pro_Nombre}} al pormayor" id="skuImg{{colorActual}}" data-color="{{colorActual}}-{{producto.Pro_IdProducto}}" data-caption="{{producto.Pro_Nombre}} {{colorActual|title}}" class=" materialboxed fixImg4 redondeo" src="{{ruta_img}}nofoto.png" >
                          {% elseif sku.SKU_Img == '' and producto.Prf_Img != '' %}
                            <img alt="{{producto.Pro_Nombre}} al pormayor" id="skuImg{{colorActual}}" data-color="{{colorActual}}-{{producto.Pro_IdProducto}}" data-caption="{{producto.Pro_Nombre}} {{colorActual|title}}" class=" materialboxed fixImg4 redondeo" src="{{ruta_img}}{{producto.Prf_Img}}" >
                          {% else %}
                            <img alt="{{producto.Pro_Nombre}} al pormayor" id="skuImg{{colorActual}}" data-color="{{colorActual}}-{{producto.Pro_IdProducto}}" data-caption="{{producto.Pro_Nombre}} {{colorActual|title}}" data-nom="{{colorActual}}" class=" materialboxed fixImg4 redondeo" src="{{ruta_img}}{{sku.SKU_Img}}" >
                          {% endif %}
                        {% set colorUnico = "" %}
                        {% set colorHex = " " %}
                        {% set break = false %}
                        {% for col in colores if not break %}
                          {% if col.SKU_Color == colorActual %}
                            <div class="colorEjemplo positionAbsolute bottom0" style="background-color:{{col.Col_Hex}}"></div>
                            {% set colorUnico = col.Col_Nombre %}
                            {% set colorHex = col.Col_Hex %}
                            {% set break = true %}
                          {% endif %}
                        {% endfor %}
                        </div>
                        <br>
                      </div> 
                      <div class="col s9 valign-wrapper sinPadding">
                        <div class="row centrarForzado">
                          <div class="col s12 sinPadding center-align">
                            <span class="fontB2 enfasisB muayuscula">{{producto.Pro_Nombre|title}}</span> <i>{{colorUnico|title}}</i>
                          </div> 
                          <div class="col s6  filaCant{{key}}" style="margin-top:8px">
                            {% set count = 0 %}
                            {% set break = false %}
                            {% for skub in SKUB|sort('asc') if not break  %}                        
                                {% if colorActual == skub.SKU_Color  %}
                                    <select name="cantidad" class="center cantSku cant{{skub.SKU_IdSku}} browser-default" id="{{skub.SKU_IdSku}}" data-idpro="{{producto.Pro_IdProducto}}" data-colresumen="{{colorHex}}" data-foto="{% if  sku.SKU_Img == '' and producto.Prf_Img == '' %}{{ruta_img}}nofoto.png{% elseif sku.SKU_Img == '' and producto.Prf_Img != '' %}{{ruta_img}}{{producto.Prf_Img}}{% else %}{{ruta_img}}{{sku.SKU_Img}}{% endif %}"  data-productname="{{producto.Pro_Nombre}}" data-productid="{{skub.SKU_IdSku}}" data-price="" xd="" data-tipovariacion="{{skub.Var_Nombre}}" data-color="{{skub.SKU_Color}}" data-variacion="{{skub.Vao_Nombre}}" data-img="{% if skub.SKU_Img %}{{skub.SKU_Img}}{% else %}{{producto.Prf_Img}}{% endif %}" data-unidad="{{skub.Unm_Nombre}}" data-idtienda="{{producto.Tie_IdTienda}}" data-minventa="{{precios[0].Prp_Desde}}" data-preciounit="" data-documento="{{producto.Pro_Documento}}" data-key="{{key}}" >
                                      <option value="-" disabled selected>Cantidad</option>
                                      <option value="0">0</option>
                                      {% for i in precios[0].Prp_Desde..500 %}<option value="{{i}}">{{i}}</option>{% endfor %}
                                    </select>
                                    <input type="number" min="0" id="precioUni{{skub.SKU_IdSku}}" class="hide center preUni" data-idsku="{{skub.SKU_IdSku}}" readonly="">
                                    <input type="number" min="0" id="precioPro{{skub.SKU_IdSku}}" class="hide center preImp" data-idsku="{{skub.SKU_IdSku}}" readonly="">
                                {% else %}
                                  {% set break = false %} 
                                {% endif %} 
                            {% set count = count + 1 %}
                            {% endfor %}
                          </div>
                          <div class="col s6 right-align precioUnitMobile precioUnitMobile{{key}} hide" style="margin-top:8px">
                              <span class='enfasisC fontB2'><span class='precioproSide'></span> c/u</span>
                              <p class='enfasisA fontD2'>S/<span class='totalproSide'></span> </p>
                          </div>
                          <br><br><br><br><br><br>
                        </div>
                      </div>
                    </div>
                  </div>
                {% endif %}
              {% endfor %}
            </div>     
          </div>
          {% endif %}    
          {% if userAgent != "PC" %}
            {% if producto.Pro_Caracteristicas %}
            <div class="divider"></div>
            <div class="row cardContentFix" id="caractBlock">
              <div class="col s12 l3">
                {% set caracteristicas = producto.Pro_Caracteristicas|split('|') %}
                <ul class="caracteristicasLista">
                  {% for cat in caracteristicas %}
                  <li class="enfasisB fontB2"><i class="positionRelative material-icons fontB2 grey-text text-darken-3">check</i>&nbsp;&nbsp;&nbsp; {{cat}}</li>
                  {% endfor %}
                </ul>
              </div>
            </div>
            {% endif %}
          {% endif %}
          <div class="divider"></div>
          <div class="row cardContentFix" id="informationBlock">
            <div class="col s12 m12 l8">
              <span class="card-title">Información del producto</span>
              {{producto.Pro_Descripcion|replace({'<figure class="image">': '', '</figure>' : '', '<img src="https://cdn.shopify.com/s/files/1/1415/1124/products/1138-Auto-Electrico-Porsche-6V-V2_1024x1024.jpg?v=1528385082">' : '', '<img src="https://cdn.shopify.com/s/files/1/1415/1124/products/1138-Auto-Electrico-Porsche-6V-V2_1024x1024.jpg?v=1528385082">': ''})}}
            </div>
            <div class="col s12 m12 l4 sinPadding hide-on-large-only">
              <br>
              <div class="divider"></div>
              <br>
            </div>
            <div class="col s12 m12 l4 sinPadding">
              <span class="card-title">Ficha del Producto</span>
              <div class="">
                <table class="striped" style="table-layout: fixed">
                  <thead class="">
                    <tr>
                      <th width="40"></th>
                      <th width="60"></th>
                    </tr>
                  </thead>
                  <tbody>
                    {% if producto.Pro_Marca %}
                      <tr>
                        <td class="enfasisB">Marca</td>
                        <td>{{producto.Pro_Marca|title}}</td>
                      </tr>
                    {% endif %}
                    {% if producto.Pro_Garantia %}
                      <tr>
                        <td class="enfasisB">Garantía</td>
                        <td>{{producto.Pro_Garantia|title}}</td>
                      </tr>
                    {% endif %}
                    {% if producto.Pro_EdoGarantia %}
                      <tr>
                        <td class="enfasisB">Estado Producto</td>
                        <td>{{producto.Pro_EdoGarantia|title}}</td>
                      </tr>
                    {% endif %}
                    <tr>
                      <td  class="enfasisB">Colores</td>
                      <td >
                        {% set arrayColores = [] %}
                        {% for col in colores %}
                          {% if col.producto_Pro_IdProducto == pro.Pro_IdProducto %}
                           {% if col.Col_Hex not in arrayColores %}
                           {% set arrayColores = arrayColores|merge([col.Col_Hex]) %}
                              {{col.Col_Nombre|title}}, 
                           {% endif %}
                          {% endif %}
                        {% endfor %}
                        {% if arrayColores is empty %}
                          <span class="flow-text notFlow enfasisB"><i>Producto único</i></span>
                        {% endif %}
                      </td>
                    </tr>
                    {% if producto.Pro_Documento %}
                      <tr>
                        <td  class="enfasisB">Documento pago</td>
                        <td >
                          {% if producto.Pro_Documento == 1 %}
                            Boleta
                          {% elseif producto.Pro_Documento == 2 %} Boleta o Factura {% endif %}
                        </td>
                      </tr>
                    {% endif %}
                    {% if producto_ficha %}
                      {% if producto_ficha.Prf_Campo1 %}
                        <tr>
                          <td class="enfasisB">Dimensiones <br>(largo x ancho x alto)</td>
                          <td>{{producto_ficha.Prf_Campo1|title}}</td>
                        </tr>
                      {% endif %}
                      {% if producto_ficha.Prf_Campo2 %}
                        <tr>
                          <td class="enfasisB">{% if producto.Cat_IdCategoria == 6 %}¿Qué contiene la caja? {% else %} Peso {% endif %}</td>
                          <td>{% if producto.Cat_IdCategoria == 6 %}{{producto_ficha.Prf_Campo2|title}}{% else %} {{producto_ficha.Prf_Campo2|title}} Kg. {% endif %}</td>
                        </tr>
                      {% endif %}
                      {% if producto_ficha.Prf_Campo3 %}
                        <tr>
                          <td class="enfasisB">{% if producto.Cat_IdCategoria == 6 %}Peso {% else %} Material {% endif %}</td>
                          <td>{% if producto.Cat_IdCategoria == 6 %}{{producto_ficha.Prf_Campo3|title}} Kg.{% else %} {{producto_ficha.Prf_Campo3|title}}{% endif %}</td>
                        </tr>
                      {% endif %}
                      {% if producto_ficha.Prf_Campo4 %}
                        <tr>
                          <td class="enfasisB">{% if producto.Cat_IdCategoria == 6 %}Potencia {% else %} Uso {% endif %}</td>
                          <td>{% if producto.Cat_IdCategoria == 6 %}{{producto_ficha.Prf_Campo4|title}} ws. {% else %} {{producto_ficha.Prf_Campo4|title}}{% endif %}</td>
                        </tr>
                      {% endif %}
                      {% if producto_ficha.Prf_Campo5 %}
                        <tr>
                          <td class="enfasisB">{% if producto.Cat_IdCategoria == 6 %}Material {% else %} Observaciones {% endif %}</td>
                          <td>{{producto_ficha.Prf_Campo5|title}}</td>
                        </tr>
                      {% endif %}
                    {% endif %}
                  </tbody>
                </table>
              </div>
            </div>
          </div>
          <br>
          <div class="divider"></div>
          <div class="row cardContentFix" id="comentsBlock">
            <div class="col s12 m12 l12 sinPadding">
                <span class="card-title">Comentarios</span>
                <div class="row">
                  <div class="col s12 m6 l6" id="divComentario">
                    {% if comentarios_respuestas %}
                    <ul class="collection">
                      {% for com in comentarios_respuestas %}
                        <li class="collection-item">
                          <article >
                            {{com.Com_Comentario}} <span class="fontB2 right">{{com.Com_FechaCreacion|date('d/m/Y')}}</span> 
                          </article>
                          {% if com.Res_Respuesta %}
                            <article class="grey-text" style="margin-left: 20px">{{com.Res_Respuesta}}
                              <span class="fontB2 right">{{com.Res_FechaCreacion|date('d/m/Y')}}</span>
                            </article>
                          {% endif %}
                        </li>
                      {% endfor %}
                      </ul>
                    {% else %}
                      <span class="grey-text">Sé el primero en preguntar y sal de dudas.</span>
                    {% endif %}
                  </div>
                  <div style="border-left: 1px solid rgba(0,0,0,0.2)" class="input-field col s12 m6 l6 center">
                    {% if usuario.logueado %}
                      {% if usuario.id_tienda != producto.Tie_IdTienda or usuario.rol == 4 %} 
                          <textarea id="comentarioPro" placeholder="Escribe tu pregunta, y recuerda que no puedes solicitar información personal a la tienda por aca" class="materialize-textarea" maxlength="100" data-length="100"></textarea>
                          <label for="comentarioPro">¿Qué necesitas saber?</label>
                          <a style="margin-left:15px" class="btn right pormayorUnico enviarComentario" data-idproducto="{{producto.Pro_IdProducto}}" data-usuario="{{usuario.id_usuario}}" data-idtienda="{{producto.Tie_IdTienda}}">Preguntar</a>
                      {% elseif usuario.id_tienda == producto.Tie_IdTienda %}
                        <a class="btn pormayorUnico" href="{{base_url()}}usuario/comentario">Ir a responder preguntas</a>
                      {% else %}
                        
                      {% endif %} 
                    {% else %}
                       <a class="btn-flat btn-flat-not transitionB  pormayorUnico-text comentLoginBtn" href="http://pormayor.pe/usuario/login">Inicia sesión para comentar</a> 
                    {% endif %}
                  </div>
                </div>
            </div>
          </div>
      </div>
      </div>
    </div>
  </main>
  {% if userAgent != "PC" %}
    <div class="bottom0 anchoTotal row centrarForzado white mobileBottomBtn" style="position:fixed;z-index: 100">
      <div class="col s12 sinPadding">
        <a  class="btn-large pormayorUnico anchoTotal  resumenMobileBtn ">VER RESUMEN</a>
      </div>
      <div class="col s6 sinPadding center hide">
        <label class="fontB2">Total de compra</label><br>
        <span class="fontC"><b>S/ <span class="totalMobileBottom"></span></b></span>
      </div>
    </div>
    <div id="resumenMobile" class="modal bottom-sheet row" style="max-height: 420px;height:100%">
      <div class="row centrarForzado">
        <div class="col s12 m12 l6  sinPadding positionRelative centrarForzado">
          <h5 class="fontA center">Resumen de producto actual</h5>
          <div class="row resumenContainer centrarForzado">
            <div class="emptyResumen">
              <div class="center col s12 m12 l12" >
                <br>
                <img src="{{ruta_img}}emptyProductos.webp" height="150"></img>
              </div>
              <div class="center col s12 m12 l12">
                <h5>Sin productos</h5>
                <span class="enfasisB">Aquí va tu lista de compras...</span>
              </div>
            </div>
            <div class="resumenPrecios">
            
            </div>
          </div>
            <div class="prevResumen align-wrapper comoBoton sinUser positionAbsolute hide"><i class=" material-icons fontA enfasisA">keyboard_arrow_up</i></div>
            <div class="nextResumen valign-wrapper comoBoton sinUser positionAbsolute hide"><i class=" material-icons fontA enfasisA">keyboard_arrow_down</i></div>
          <div class="row centrarForzado">
            {% if usuario.id_tienda != producto.Tie_IdTienda %}
              <div class="col s7 sinPadding center valign-wrapper wrapperPrecioMobile">
                <a class="centrarForzado btn green add_cart sinHover" data-totalc="{{totalc}}">AGREGAR AL CARRITO</a>
              </div>
              <div class="col s5 sinPadding right-align valign-wrapper wrapperPrecioMobile positionRelative">
                <span class="centrarForzado fontB2 positionAbsolute "><strong id="totalcantidad">0</strong> unidades</span>
                <span class="centrarForzado">S/. <strong class="fontC" id="totalImporte">0</strong></span>
              </div>
            {% endif %}
          </div>
        </div>
      </div>
    </div>
  {% endif %}
  <!-- Aviso de agregado -->
  <div id="modal_aviso" class="modal">
    <div class="modal-content validar sinPadding white">
      <a class="modal-close modal-close-custom right"><i class="material-icons  closeIconA">close</i></a>
      <h5 class="center centrarForzado white-text z-depth-2 green">Agregado al carrito</h5>
      <div class="aviso_productos">
        <div class="row">
          <br>
          <div class="center col s12 m12 l2">
            <img class="fixImg4 redondeo imgAviso" src=""> 
          </div>
          <div class="left-align col s12 m12 l8">
            <span class="fontB2"><b class="nombreAviso"></b></span><br>
            <span class="fontB2 enfasisB cantAviso"></span><br>
            <span class="fontB2">Sub-total <span class="fontA pormayorUnico-text totalAviso"></span><br>
          </div>
          <div class="center col s12 m12 l12">
            <br><br>
            <a class="btn pormayorUnico center verCarrito" href="{{base_url()}}productos/carrito">Ir al carrito</a><a class="btn-flat center modal-close "><u>Seguir comprando</u></a>
          </div>
        </div>
      </div> 
    </div>
  </div>

  {% if usuario.id_tienda != tienda.Tie_IdTienda %}
  <!-- CARRITO DE COMPRA -->
  <div id="modal_resumenCotizacion" class="modal">
    <div class="modal-content validar modalCustomPro white">
      <a class="modal-close modal-close-custom tooltipped right" data-dismiss="modal" data-position="bottom" data-tooltip="Cerrar ventana"><i class="material-icons  closeIconA">close</i></a>
      <h5 class="headerModal z-depth-2 colorPrincipal">Mi compra</h5>
      <div id="cart_details">
        <div class="row">
          <div class="center col s12 m12 l12" style="margin-top: 20px;">
            <img alt="Pormayor.pe - carrito vacio" src="{{ruta_img}}emptyCart.webp" height="150"></img>
          </div>
          <div class="center col s12 m12 l12">
            <h5>Sin productos agregados</h5>
            <p class="enfasisB">No ha agregado productos en su carrito</p>
          </div>
          <div class="center col s12 m12 l12">
            <a class="btn center modal-close">Ir a comprar</a>
          </div>
        </div>
      </div>  
    </div>
  </div>
  {% endif %}  
{% endblock %}
  

{% block script %}
  <script>
  var rando_precio = {{precios|json_encode()}};
  var flagVariaciones = false;
  function selCostoEnvio(val) {

    var dis = val;
    var cant = $("#cantEnvio").val();
    if (cant == null) {
      cant = 1;
    }
    // var cant = $("#cantEnvio").val();
    var dim = {{producto_ficha.Prf_Campo1|json_encode()}};
    var pes = {{producto_ficha.Prf_Campo3|json_encode()}};
  
      $.post(base_url+'productos/selCostoEnvio2',{"dis":dis,"cant":cant,"dim":dim,"pes":pes},function(data){
        var result=JSON.parse(data);

        if(result) {
          var d1=result['cenv']['Dia_DiaEnvio'];
          var d2=(parseInt(result['cenv']['Dia_DiaEnvio']) + 1);
          $('.tiempoEntrega').text('Tiempo de entrega de '+d1+' a '+d2+' días ('+$( "#selecRegion option:selected" ).text()+' - '+$( "#selDistrito option:selected" ).text()+')');
          var costo=Math.round(result['costoReal']);
          $('.cEnv').text(parseFloat(costo).toFixed(2));  
        } else {
          $('.cEnv4').text('0.00');
          // $('.noenv').text('NO HACEMOS ENVÍO A ESE DISTRITO');
        }
      });
}
  </script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/sticky-kit/1.1.3/sticky-kit.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.6/umd/popper.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/animejs/2.2.0/anime.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/easyzoom-mp@2.2.2/dist/easyzoom.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.9.0/slick.min.js"></script>
  <script src="{{base_url('public/')}}js/tienda/cart_cotizacion.js?v={{now|date('i:s')}}"></script>
  <script src="{{base_url('public/')}}js/productos/favoritos.js?v={{now|date('i:s')}}"></script>

{% endblock %}